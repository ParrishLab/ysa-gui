name: Release

on:
  release:
    types: [published]

jobs:
  test-before-release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y xvfb libgl1-mesa-glx libegl1-mesa libdbus-1-3

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt

    - name: Run full test suite
      run: |
        xvfb-run -a pytest tests/ -v --cov=src --cov-report=xml

    - name: Upload test results
      if: failure()
      uses: actions/upload-artifact@v3
      with:
        name: test-results
        path: coverage.xml

  build-artifacts:
    needs: test-before-release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build executable (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        pyinstaller --onefile --windowed \
          --name "YSA-GUI-${{ matrix.os }}" \
          --add-data "src:src" \
          --hidden-import PyQt5.QtCore \
          --hidden-import PyQt5.QtGui \
          --hidden-import PyQt5.QtWidgets \
          --hidden-import pyqtgraph \
          --hidden-import numpy \
          --hidden-import scipy \
          --hidden-import h5py \
          src/main.py

    - name: Build executable (Windows)
      if: runner.os == 'Windows'
      run: |
        pyinstaller --onefile --windowed `
          --name "YSA-GUI-Windows" `
          --add-data "src;src" `
          --hidden-import PyQt5.QtCore `
          --hidden-import PyQt5.QtGui `
          --hidden-import PyQt5.QtWidgets `
          --hidden-import pyqtgraph `
          --hidden-import numpy `
          --hidden-import scipy `
          --hidden-import h5py `
          src/main.py

    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: ysa-gui-${{ matrix.os }}
        path: dist/

  create-release:
    needs: build-artifacts
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v3

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          ysa-gui-ubuntu-latest/*
          ysa-gui-windows-latest/*
          ysa-gui-macos-latest/*
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}